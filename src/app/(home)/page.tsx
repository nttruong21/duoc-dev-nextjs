// Core
import { Metadata } from "next";

// Metadata
export const metadata: Metadata = {
  title: "Home",
};

// Component
const Home = async () => {
  return <div>Home</div>;
};

export default Home;
